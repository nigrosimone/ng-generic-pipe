# ng-generic-pipe example

Minimal Angular app using `ng-generic-pipe` from npm. Run it in the browser:
**https://stackblitz.com/github/nigrosimone/ng-generic-pipe/tree/main/examples/stackblitz**

Locally: `npm install && npm start`.
